image:
  file: .gitpod.Dockerfile
tasks:
  - before: |
      source /home/gitpod/.ghcup/env
  - init: |
      cabal update
      stack build

    command: |
      stack test

vscode:
  extensions:
    - haskell.haskell
    - justusadam.language-haskell
